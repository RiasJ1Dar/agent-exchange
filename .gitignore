/target
**/*.db
**/*.db-wal
**/*.db-shm
.env
.env.*

# Тимчасова тека збірки для сухих прогонок G0.
# Не /target — саме тому /target її НЕ ловив: 131 МБ ледь не поїхали в коміт.
/target-g0/

# Локальна реєстрація MCP: абсолютні шляхи й імʼя користувача.
# Зразок для інших машин класти окремо, як .mcp.json.example
.mcp.json
