//! `exchange-ui` — переглядач стану обміну для **людини**, а не для агентів.
//!
//! Крейт свідомо окремий від `mcp`: єдиний UI всередині сервера означав би,
//! що хтось мусить тримати процес запущеним, а два процеси не сядуть на один
//! порт. Тут транспорт (`http`), складання сторінки (`page`) і читання бази
//! **лише на читання** (`db`).

pub mod db;
pub mod http;
pub mod page;
